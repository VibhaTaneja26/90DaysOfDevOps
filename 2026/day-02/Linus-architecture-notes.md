Top 5 commands
# List files 
ls -l   (Lists all details of files like permissions,owner,group,user)

ls -a  (Lists all files along with hidden files (.file)

# Change Directory
cd /path 
cd ..              ( to go to previous directory)
cd /home/ubuntu    ( change directory to given path)

# Present Working Directory
pwd

# Show processes
ps aux  (ps is used to show all running processes)

# systemctl  (Manage services)
to start, stop, status
syatemctl start nginx  ( to start service)
systemctl stop nginx   (to stop nginx)
systemctl status nginx (to check status)

# top  (Monitor CPU and memory)
# htop (real time CPU and memory)
# df -h (Check disk space)
# free -h (check memory)
# grep (search text)
# cat ( to see contents of files)



# 
